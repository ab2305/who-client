/*
	configuration 관리

	ex)
	images.js // 다양한 이미지에 대한 경로 관리
	routes.js // 모든 경로에 액세스 할 수 있도록 단일 장소 제공
	setting.js // server URL과 같은 정보를 갖고 있음
	styles.js // global color 등 변수와 같은 것들 관리
	...

	images.js example)

	const images = {
		logo: require('../images/rn-logo.png')
	}

	export default images
*/
